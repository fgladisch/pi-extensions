---
"@fgladisch/pi-welcome-message": minor
---

Make welcome sections configurable via `~/.pi/agent/settings.json` using `welcomeMessage.sections`.

Also rename section keys to `nodePackage` and `piResources` (with `git` unchanged), and document configuration in the package README.

---
"@fgladisch/pi-bash-approval": patch
---

Treat background `&` and grouping parentheses as shell syntax during bash approval splitting so grouped dev-server commands suggest clean allow-list rules.
